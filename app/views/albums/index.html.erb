<p style="color: green"><%= notice %></p>

           <%= link_to "New album", new_album_path ,class: "btn btn-secondary"%>

<%=render partial: "albums/def"%><br>
<div id="albums">

  <% @albums.each do |album| %>
  <%if album != Album.first%>
    <%= render album %>
      <%= link_to "Add Photos to this album", album ,class: "btn btn-secondary" %><br>
      <div id = "photo">
      <%# <%album = album.order(position: :asc)%> 
 <%album.photos.order(position: :asc).each do |photo|%>

  <%= render "photos/draggable",photo: photo,album: album%>
    <%# <%=render partial: "photos/draggable", album: album%> 
    <%# <%if a.image.attached?%> 
         
              <%# <%=image_tag a.image,width: "200px"%> 
              
        
    <%# <%end%> 
    <%end%>
    </div>
    <%end%>

    <style>

     #photo {
    display: flex;
    flex-wrap: wrap;
  }

    </style>
  

  
    
  
  <% end %>




</div>

<%# <%= link_to "New album", new_album_path %> 
